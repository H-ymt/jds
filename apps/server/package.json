{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "hono": "catalog:",
    "@hono/node-server": "^1.14.4",
    "@trpc/server": "catalog:",
    "@hono/trpc-server": "^0.4.0",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@cfs/env": "workspace:*",
    "@cfs/api": "workspace:*",
    "@cfs/db": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "@types/node": "^22.13.11",
    "typescript": "catalog:",
    "tsdown": "^0.16.5",
    "@cfs/config": "workspace:*"
  }
}
